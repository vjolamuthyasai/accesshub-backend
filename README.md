# accesshub-backend
Backend service for AccessiHub, an accessibility analyzer platform. Built with Flask, it scans websites for WCAG compliance issues, processes HTML, and generates structured reports via REST APIs for the frontend dashboard.
